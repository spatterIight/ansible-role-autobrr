# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Verify
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Wait for autobrr service to become active (up to 180s)
      ansible.builtin.systemd:
        name: autobrr.service
      register: svc
      until: svc.status.ActiveState == 'active'
      retries: 36
      delay: 5

    - name: Assert autobrr service is active
      ansible.builtin.assert:
        that: svc.status.ActiveState == 'active'
        fail_msg: "autobrr.service not active ({{ svc.status.ActiveState }})"
        success_msg: "autobrr.service is active"

    - name: Wait for autobrr HTTP endpoint to respond (up to 10m)
      ansible.builtin.uri:
        url: "http://localhost:7474/"
        method: GET
        status_code: [200, 302]
        follow_redirects: none
      register: ep
      until: ep.status in [200, 302]
      retries: 120
      delay: 5

    - name: Assert autobrr HTTP endpoint responds
      ansible.builtin.assert:
        that: ep.status in [200, 302]
        fail_msg: "autobrr did not respond as expected ({{ ep.status | default('no response') }})"
        success_msg: "autobrr responded with {{ ep.status }}"

    - name: Show service status summary
      ansible.builtin.debug:
        msg:
          - "autobrr systemd: {{ svc.status.ActiveState }}"
          - "autobrr HTTP: {{ ep.status }}"
